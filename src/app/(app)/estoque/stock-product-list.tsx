"use client";

import { Pencil, Search, X } from "lucide-react";
import { useDeferredValue, useMemo, useState } from "react";

import { updateRetailVariant } from "@/actions/retail";
import { ActionForm } from "@/components/action-form";

export type StockProductRow = { id: string; variantId: string; name: string; presentation: string; rawPresentation: string; brand: string; rawBrand: string; sku: string; barcode: string; unit: string; description: string; costValue: string; saleValue: string; minimumValue: string; quantity: number; quantityLabel: string; costUnit: string; costTotal: string; saleUnit: string; saleTotal: string; minimum: number; categoryId: string | null; category: string; subcategoryId: string | null; subcategory: string; hasConsumption: boolean };
const normalize = (value: string) => value.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase();

function EditProductButton({ product, categories, subcategories }: { product: StockProductRow; categories: Array<{ id: string; name: string }>; subcategories: Array<{ id: string; categoryId: string; name: string }> }) {
  const [open, setOpen] = useState(false); const [categoryId, setCategoryId] = useState(product.categoryId ?? "");
  const filteredSubcategories = subcategories.filter((item) => item.categoryId === categoryId);
  return <><button className="icon-button" type="button" onClick={() => setOpen(true)} aria-label={`Editar ${product.name}`}><Pencil className="size-4" /></button>{open && <div className="fixed inset-0 z-50 overflow-y-auto bg-slate-950/60 p-4" role="dialog" aria-modal="true" aria-label={`Editar ${product.name}`}><div className="panel mx-auto my-6 w-full max-w-3xl"><div className="flex justify-between"><h2 className="text-xl font-extrabold">Editar produto</h2><button className="icon-button" type="button" onClick={() => setOpen(false)}><X className="size-5" /></button></div><ActionForm action={updateRetailVariant} successMessage="Produto atualizado." className="mt-5 grid gap-3 sm:grid-cols-2" onSuccess={() => setOpen(false)}><input type="hidden" name="variantId" value={product.variantId} /><input type="hidden" name="isActive" value="on" /><label className="grid gap-1 text-sm font-bold">Nome<input className="field" name="name" required defaultValue={product.name} /></label><label className="grid gap-1 text-sm font-bold">Apresentação<input className="field" name="variantName" defaultValue={product.rawPresentation} /></label><label className="grid gap-1 text-sm font-bold">Marca<input className="field" name="brand" defaultValue={product.rawBrand} /></label><label className="grid gap-1 text-sm font-bold">Categoria<select className="field" name="categoryId" value={categoryId} onChange={(event) => setCategoryId(event.target.value)}><option value="">Sem categoria</option>{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="grid gap-1 text-sm font-bold">Subcategoria<select className="field" name="subcategoryId" defaultValue={product.subcategoryId ?? ""} disabled={!categoryId}><option value="">Sem subcategoria</option>{filteredSubcategories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="grid gap-1 text-sm font-bold">Unidade<select className="field" name="unit" defaultValue={product.unit}><option value="unit">Unidade</option><option value="ml">Mililitro</option><option value="g">Grama</option><option value="kg">Quilograma</option><option value="l">Litro</option><option value="dose">Dose</option></select></label><label className="grid gap-1 text-sm font-bold">Custo unitário<input className="field" name="cost" inputMode="decimal" defaultValue={product.costValue} /></label><label className="grid gap-1 text-sm font-bold">Preço de venda<input className="field" name="salePrice" inputMode="decimal" defaultValue={product.saleValue} /></label><label className="grid gap-1 text-sm font-bold">Estoque mínimo<input className="field" name="minimumQuantity" inputMode="decimal" defaultValue={product.minimumValue} /></label><label className="grid gap-1 text-sm font-bold">SKU<input className="field" name="sku" defaultValue={product.sku} /></label><label className="grid gap-1 text-sm font-bold">Código de barras<input className="field" name="barcode" defaultValue={product.barcode} /></label><label className="grid gap-1 text-sm font-bold sm:col-span-2">Descrição<textarea className="field min-h-20" name="description" defaultValue={product.description} /></label><button className="primary-button sm:col-span-2">Salvar alterações</button></ActionForm></div></div>}</>;
}

export function StockProductList({ products, categories, subcategories, canManage }: { products: StockProductRow[]; categories: Array<{ id: string; name: string }>; subcategories: Array<{ id: string; categoryId: string; name: string }>; canManage: boolean }) {
  const [query, setQuery] = useState(""); const deferredQuery = useDeferredValue(query);
  const [status, setStatus] = useState("all"); const [categoryId, setCategoryId] = useState(""); const [subcategoryId, setSubcategoryId] = useState("");
  const filteredSubcategories = subcategories.filter((item) => item.categoryId === categoryId);
  const filtered = useMemo(() => products.filter((item) => {
    const matchesQuery = normalize(item.name).includes(normalize(deferredQuery));
    const matchesStatus = status === "all" || (status === "stock" && item.quantity > 0) || (status === "consumption" && item.hasConsumption) || (status === "low" && item.quantity <= item.minimum);
    return matchesQuery && matchesStatus && (!categoryId || item.categoryId === categoryId) && (!subcategoryId || item.subcategoryId === subcategoryId);
  }), [products, deferredQuery, status, categoryId, subcategoryId]);
  return <section className="panel mt-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="text-lg font-extrabold">Produtos</h2><p className="text-sm text-muted">Consulte saldos, custos e valores de venda.</p></div><label className="relative w-full sm:max-w-sm"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted" /><input className="field w-full pl-9" type="search" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar por nome do produto" /></label></div><div className="mt-4 grid gap-2 sm:grid-cols-3"><select className="field" value={status} onChange={(event) => setStatus(event.target.value)}><option value="all">Todos os produtos</option><option value="stock">Produtos em estoque</option><option value="consumption">Em consumo na loja</option><option value="low">Abaixo do estoque mínimo</option></select><select className="field" value={categoryId} onChange={(event) => { setCategoryId(event.target.value); setSubcategoryId(""); }}><option value="">Todas as categorias</option>{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><select className="field" value={subcategoryId} onChange={(event) => setSubcategoryId(event.target.value)} disabled={!categoryId}><option value="">Todas as subcategorias</option>{filteredSubcategories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[1100px] text-left text-sm"><thead className="border-b text-xs uppercase text-muted"><tr><th className="p-3">Produto</th><th className="p-3">Apresentação</th><th className="p-3">Marca</th><th className="p-3 text-right">Qtd.</th><th className="p-3 text-right">Custo unit.</th><th className="p-3 text-right">Custo total</th><th className="p-3 text-right">Venda unit.</th><th className="p-3 text-right">Venda total</th>{canManage && <th className="p-3"><span className="sr-only">Ações</span></th>}</tr></thead><tbody className="divide-y">{filtered.map((item) => <tr key={item.id}><td className="p-3"><strong>{item.name}</strong><p className="text-xs text-muted">{[item.category, item.subcategory].filter(Boolean).join(" · ") || "Sem categoria"}</p></td><td className="p-3">{item.presentation}</td><td className="p-3">{item.brand}</td><td className={`p-3 text-right font-bold ${item.quantity <= item.minimum ? "text-red-700" : ""}`}>{item.quantityLabel}</td><td className="p-3 text-right">{item.costUnit}</td><td className="p-3 text-right">{item.costTotal}</td><td className="p-3 text-right">{item.saleUnit}</td><td className="p-3 text-right font-bold text-brand">{item.saleTotal}</td>{canManage && <td className="p-3"><EditProductButton product={item} categories={categories} subcategories={subcategories} /></td>}</tr>)}</tbody></table>{!filtered.length && <p className="py-8 text-center text-sm text-muted">Nenhum produto encontrado para os filtros selecionados.</p>}</div></section>;
}
